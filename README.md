# FunctionalLevelTestPOC
A POC for functional tests with python/robot that mocks a third party API with Flask

To install:
pip install -r requirements.txt
pip install -e .

To run all tests:
robot FunctionalTestingPOC
